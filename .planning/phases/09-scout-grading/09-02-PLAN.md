---
phase: 09-scout-grading
plan: 02
type: execute
---

<objective>
Create comprehensive grading form component with all evaluation fields.

Purpose: Enable scouts to evaluate players with status, recommendations, ratings, and notes.
Output: Working GradingForm.tsx component with full form UI.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./09-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/grades.ts
@src/types/player.ts

**Prior plan output:**
- PlayerGrade interface in lib/grades.ts
- CATEGORY_LABELS constant
- saveGrade() helper function
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StarRating sub-component</name>
  <files>src/components/StarRating.tsx</files>
  <action>
Create reusable star rating component:

```typescript
'use client';

interface StarRatingProps {
  value: number;
  onChange: (value: number) => void;
  label: string;
}

export function StarRating({ value, onChange, label }: StarRatingProps) {
  return (
    <div className="flex items-center gap-2">
      <span className="text-sm text-zinc-600 dark:text-zinc-400 w-20">{label}</span>
      <div className="flex gap-1">
        {[1, 2, 3, 4, 5].map((star) => (
          <button
            key={star}
            type="button"
            onClick={() => onChange(star)}
            className="text-xl focus:outline-none focus:ring-2 focus:ring-blue-500 rounded"
          >
            {star <= value ? '★' : '☆'}
          </button>
        ))}
      </div>
    </div>
  );
}
```

Style stars with yellow/gold color when filled: text-yellow-500 for filled, text-zinc-400 for empty.
Add hover state to show preview of selection.
  </action>
  <verify>Component renders without errors when imported</verify>
  <done>StarRating component created with clickable stars and label</done>
</task>

<task type="auto">
  <name>Task 2: Create GradingForm component with all sections</name>
  <files>src/components/GradingForm.tsx</files>
  <action>
Create comprehensive grading form with these sections:

Props: { player: { id, name, position, club }, existingGrade?: PlayerGrade, onSave: () => void }

FORM SECTIONS:

1. Status & Recommendation section:
   - STATUS dropdown: FM | U23 | LOAN | WATCH
   - RECOMMENDATION: 3 toggle buttons (Sign=green, Monitor=yellow, Discard=red)
   - SCOUTING LEVEL dropdown: Basic | Impressive | Data only

2. Player Category section:
   - Dropdown with CATEGORY_LABELS (1-8)
   - Show: "1 - Liga 1a Player", "2 - Relegated Player", etc.

3. Ability Ratings section:
   - Current Ability: dropdown 1.0 to 5.0, step 0.5
   - Potential: dropdown 1.0 to 5.0, step 0.5

4. Star Ratings section (use StarRating component):
   - Technical ★★★☆☆
   - Tactical ★★★★☆
   - Physical ★★☆☆☆
   - Mental ★★★★★

5. Notes section:
   - Textarea with placeholder "Scout observations, concerns, strengths..."

6. Optional section:
   - Transfer Fee: text input, placeholder "€500k - €1M"
   - Salary: text input, placeholder "€3k/month"

7. Save button:
   - "Save Grade" (or "Update Grade" if existingGrade provided)
   - On save: call saveGrade(), show success toast, call onSave()

Use React useState for form state. Initialize from existingGrade if provided.
Style with Tailwind matching existing app design (zinc colors, rounded-lg, shadows).
  </action>
  <verify>Component compiles: cd /Users/ocoulibaly/Desktop/bacau-scout && npm run build</verify>
  <done>GradingForm component with all 6 sections, form state management, and save functionality</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] StarRating component renders clickable stars
- [ ] GradingForm shows all 6 sections
- [ ] Form state initializes from existingGrade prop
- [ ] Save button calls saveGrade and onSave callback
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Form matches design spec (dropdowns, toggle buttons, stars, textarea)
</success_criteria>

<output>
After completion, create `.planning/phases/09-scout-grading/09-02-SUMMARY.md`
</output>
