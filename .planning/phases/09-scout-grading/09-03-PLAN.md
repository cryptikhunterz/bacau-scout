---
phase: 09-scout-grading
plan: 03
type: execute
---

<objective>
Integrate grading form into player profile page with edit/delete capability.

Purpose: Allow scouts to grade players directly from their profile page.
Output: Player profile page with "Scout Evaluation" section containing GradingForm.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./09-03-SUMMARY.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/player/[id]/page.tsx
@src/lib/grades.ts
@src/components/GradingForm.tsx

**Prior plan outputs:**
- PlayerGrade interface and CRUD helpers
- GradingForm component with all sections
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client wrapper for player profile grading</name>
  <files>src/components/PlayerGrading.tsx</files>
  <action>
Create client component wrapper for grading functionality:

```typescript
'use client';

import { useState, useEffect } from 'react';
import { GradingForm } from './GradingForm';
import { getGrade, deleteGrade, PlayerGrade } from '@/lib/grades';

interface PlayerGradingProps {
  player: {
    id: string;
    name: string;
    position: string;
    club: string;
  };
}

export function PlayerGrading({ player }: PlayerGradingProps) {
  const [existingGrade, setExistingGrade] = useState<PlayerGrade | null>(null);
  const [showDeleteConfirm, setShowDeleteConfirm] = useState(false);

  useEffect(() => {
    setExistingGrade(getGrade(player.id));
  }, [player.id]);

  const handleSave = () => {
    setExistingGrade(getGrade(player.id));
  };

  const handleDelete = () => {
    deleteGrade(player.id);
    setExistingGrade(null);
    setShowDeleteConfirm(false);
  };

  return (
    <div className="bg-white dark:bg-zinc-800 rounded-lg shadow-sm border border-zinc-200 dark:border-zinc-700 p-6">
      <h2 className="text-lg font-semibold text-zinc-900 dark:text-white mb-4">
        Scout Evaluation
      </h2>

      {existingGrade && (
        <p className="text-sm text-zinc-500 mb-4">
          Last graded: {new Date(existingGrade.gradedAt).toLocaleDateString()}
        </p>
      )}

      <GradingForm
        player={player}
        existingGrade={existingGrade || undefined}
        onSave={handleSave}
      />

      {existingGrade && (
        <div className="mt-6 pt-4 border-t border-zinc-200 dark:border-zinc-700">
          {!showDeleteConfirm ? (
            <button
              onClick={() => setShowDeleteConfirm(true)}
              className="text-red-600 hover:text-red-700 text-sm"
            >
              Delete Grade
            </button>
          ) : (
            <div className="flex items-center gap-3">
              <span className="text-sm text-zinc-600">Delete this grade?</span>
              <button onClick={handleDelete} className="text-red-600 text-sm font-medium">
                Yes, delete
              </button>
              <button onClick={() => setShowDeleteConfirm(false)} className="text-zinc-500 text-sm">
                Cancel
              </button>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>Component compiles without errors</verify>
  <done>PlayerGrading wrapper with grade loading, save callback, and delete confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Add PlayerGrading to player profile page</name>
  <files>src/app/player/[id]/page.tsx</files>
  <action>
Modify player profile page to include Scout Evaluation section:

1. Import PlayerGrading component

2. Add after the stats table section (before External Links):
```tsx
{/* Scout Evaluation */}
<PlayerGrading
  player={{
    id: player.id,
    name: player.name,
    position: player.position,
    club: player.club,
  }}
/>
```

3. The layout order should be:
   - Back button
   - Player header (name, position, market value)
   - Player Information grid
   - Career Totals (if exists)
   - Performance by Season table
   - Scout Evaluation section (NEW)
   - External Links (Transfermarkt)

Keep existing page structure intact, just add the new section.
  </action>
  <verify>Build succeeds and dev server shows grading form on player profile</verify>
  <done>Player profile page includes Scout Evaluation section with GradingForm</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Grading form integrated into player profile page</what-built>
  <how-to-verify>
    1. Run: cd /Users/ocoulibaly/Desktop/bacau-scout && npm run dev
    2. Visit: http://localhost:3000
    3. Search for "Suciu" and click the result
    4. Scroll down to see "Scout Evaluation" section
    5. Verify form shows all fields:
       - Status dropdown (FM/U23/LOAN/WATCH)
       - Recommendation buttons (Sign/Monitor/Discard)
       - Scouting Level dropdown
       - Player Category dropdown
       - Ability/Potential dropdowns
       - Star ratings (Technical/Tactical/Physical/Mental)
       - Notes textarea
       - Transfer Fee and Salary inputs
       - Save button
    6. Fill in a grade and save
    7. Refresh page - grade should persist
    8. Test delete functionality
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Player profile shows Scout Evaluation section
- [ ] Grading form displays all fields
- [ ] Save persists to localStorage
- [ ] Delete removes grade with confirmation
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verification approved
- Grading form works end-to-end on player profile
</success_criteria>

<output>
After completion, create `.planning/phases/09-scout-grading/09-03-SUMMARY.md`
</output>
