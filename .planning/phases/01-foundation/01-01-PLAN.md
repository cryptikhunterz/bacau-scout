---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Next.js 14 project with Tailwind CSS and Supabase client configured.

Purpose: Establish the foundation that all other phases build upon - a working app with database connectivity.
Output: Working Next.js app that can connect to Supabase, ready for data layer implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Infrastructure ready (from PROJECT.md):**
- Supabase project: fuubyhubptalxwondwov.supabase.co
- Data file: ~/Desktop/Bacau scout prototype /JSON 2.json

**Tech stack (from PROJECT.md constraints):**
- Next.js 14 + App Router
- Tailwind CSS
- Supabase (Postgres)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 14 project with Tailwind CSS</name>
  <files>package.json, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
    Create Next.js 14 project in current directory using create-next-app with these options:
    - TypeScript: yes
    - ESLint: yes
    - Tailwind CSS: yes
    - App Router: yes
    - src/ directory: yes
    - Import alias: @/*

    Run: `npx create-next-app@latest . --typescript --eslint --tailwind --app --src-dir --import-alias "@/*" --use-npm`

    If directory not empty, may need --no-git flag or handle existing .planning/ folder.

    After creation, verify the basic app runs with `npm run dev`.
  </action>
  <verify>npm run dev starts without errors, http://localhost:3000 shows Next.js welcome page</verify>
  <done>Next.js 14 app with Tailwind CSS runs successfully on localhost:3000</done>
</task>

<task type="auto">
  <name>Task 2: Configure Supabase client</name>
  <files>src/lib/supabase.ts, .env.local, src/app/page.tsx</files>
  <action>
    1. Install Supabase client: `npm install @supabase/supabase-js`

    2. Create src/lib/supabase.ts with client initialization:
       - Export createClient function for server components
       - Use environment variables for URL and anon key
       - TypeScript types will be added in Phase 2 after schema exists

    3. Create .env.local with Supabase credentials:
       - NEXT_PUBLIC_SUPABASE_URL=https://fuubyhubptalxwondwov.supabase.co
       - NEXT_PUBLIC_SUPABASE_ANON_KEY=[will need from Supabase dashboard]

    4. Update src/app/page.tsx to:
       - Import supabase client
       - Make a simple test query (e.g., supabase.from('_test_').select() - will fail but proves connection)
       - Display connection status on homepage

    Note: The anon key needs to be retrieved from Supabase dashboard. If not available, create .env.local.example with placeholder and note in summary.
  </action>
  <verify>npm run build succeeds, app loads without Supabase import errors</verify>
  <done>Supabase client configured, .env.local created (or .env.local.example if key unavailable), app builds successfully</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Tailwind CSS works (test by adding a colored bg class)
- [ ] Supabase client imports without errors
- [ ] .env.local (or .env.local.example) exists with Supabase URL
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Next.js 14 app runs with Tailwind CSS
- Supabase client configured and importable
- Phase 1 complete, ready for Phase 2 (Data Layer)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following summary template.
</output>
